import React from 'react';

export default function Header(): JSX.Element {
  return (
    <header>
      {/* The content of the header will be populated by Docusaurus's navbar */}
    </header>
  );
}
